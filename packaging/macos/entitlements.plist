<?xml version="1.0" encoding="UTF-8"?>
<!--
  Entitlements for the DIRECT-DOWNLOAD (.dmg) build, signed with a
  "Developer ID Application" certificate under the hardened runtime.

  The three cs.* exceptions are what a PyInstaller/CPython bundle needs: the
  interpreter allocates writable-executable memory and loads extension modules
  that are signed with a different identity than the outer bundle.

  NOTE: this is NOT the Mac App Store set — see entitlements-mas.plist.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>
</dict>
</plist>
